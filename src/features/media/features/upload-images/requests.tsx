export const getSignedUrl = async (file: File) => {
  console.log(file.type);
  // const signedUrl = await fetch('/api/storage-gcs-generate-signed-url', {
  //   method: 'POST',
  //   body: JSON.stringify({ file }),
  // });
  // return signedUrl;
};
